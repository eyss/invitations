import { Readable, Unsubscriber } from "svelte/store";
import { ReactiveController, ReactiveElement } from "lit";
/**
 * Tracks a changing store, derived at each update
 */
export declare class StoreSubscriber<V> implements ReactiveController {
    protected host: ReactiveElement;
    protected getStore: () => Readable<V>;
    value: V;
    _unsubscribe: Unsubscriber | undefined;
    private _previousStore;
    constructor(host: ReactiveElement, getStore: () => Readable<V>);
    hostUpdate(): void;
    hostDisconnected(): void;
    unsubscribe(): void;
    shouldResubscribe(store: Readable<V>): boolean;
    resubscribe(): void;
}
